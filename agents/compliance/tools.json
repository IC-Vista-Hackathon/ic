{
  "tools": [
    {
      "type": "function",
      "function": {
        "name": "run_compliance_check",
        "description": "Evaluate a BillerConfiguration against publish policy (fee disclosure, required text, payment-method rules) and return structured pass/fail findings. Backed by the rules engine. This evaluates only; it does not persist the 'compliance' field or publish — the publish endpoint re-runs this server-side and writes the result itself.",
        "parameters": {
          "type": "object",
          "properties": {
            "config": {
              "type": "object",
              "description": "The BillerConfiguration version to evaluate.",
              "additionalProperties": true
            }
          },
          "required": ["config"]
        }
      }
    }
  ]
}
