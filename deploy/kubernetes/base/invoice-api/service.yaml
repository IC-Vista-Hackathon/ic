apiVersion: v1
kind: Service
metadata:
  name: ic-invoice-api
  labels:
    app: ic-invoice-api
spec:
  type: ClusterIP
  selector:
    app: ic-invoice-api
  ports:
    - name: http
      port: 8080
      targetPort: http
