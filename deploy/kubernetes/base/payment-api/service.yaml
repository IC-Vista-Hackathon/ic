apiVersion: v1
kind: Service
metadata:
  name: ic-payment-api
  labels:
    app: ic-payment-api
spec:
  type: ClusterIP
  selector:
    app: ic-payment-api
  ports:
    - name: http
      port: 8080
      targetPort: http
