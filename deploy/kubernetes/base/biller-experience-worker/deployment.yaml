apiVersion: apps/v1
kind: Deployment
metadata:
  name: ic-biller-experience-worker
  labels:
    app: ic-biller-experience-worker
    app.kubernetes.io/part-of: ic
spec:
  # Single publishing worker; it owns idempotent publication jobs.
  replicas: 1
  selector:
    matchLabels:
      app: ic-biller-experience-worker
  template:
    metadata:
      labels:
        app: ic-biller-experience-worker
    spec:
      serviceAccountName: ic-workload
      containers:
        - name: biller-experience-worker
          image: acrichackjk4zmntatjem4.azurecr.io/ic-biller-experience-worker:latest
          ports:
            - name: http
              containerPort: 8080
          readinessProbe:
            httpGet:
              path: /health/ready
              port: http
            initialDelaySeconds: 3
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health/live
              port: http
            initialDelaySeconds: 5
            periodSeconds: 15
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            seccompProfile:
              type: RuntimeDefault
