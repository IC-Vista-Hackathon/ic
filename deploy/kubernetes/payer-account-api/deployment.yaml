apiVersion: apps/v1
kind: Deployment
metadata:
  name: ic-payer-account-api
  namespace: ic
  labels:
    app: ic-payer-account-api
    app.kubernetes.io/part-of: ic
spec:
  # In-memory stores: replicas MUST stay 1 until Cosmos lands — with 2+, seeds
  # land on one pod and lookups hit the other (verified live 2026-07-14).
  replicas: 1
  selector:
    matchLabels:
      app: ic-payer-account-api
  template:
    metadata:
      labels:
        app: ic-payer-account-api
    spec:
      # Platform-provisioned SA (federated to the workload UAMI). No Azure
      # dependency yet — in-memory store; Cosmos comes later.
      serviceAccountName: ic-workload
      containers:
        - name: payer-account-api
          image: acrichackjk4zmntatjem4.azurecr.io/ic-payer-account-api:v1
          ports:
            - name: http
              containerPort: 8080
          readinessProbe:
            httpGet:
              path: /health/ready
              port: http
            initialDelaySeconds: 3
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health/live
              port: http
            initialDelaySeconds: 5
            periodSeconds: 15
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            seccompProfile:
              type: RuntimeDefault
